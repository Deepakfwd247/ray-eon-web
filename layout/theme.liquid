<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title }} | Ray-eon</title>
    {{ content_for_header }}
    {{ 'style.css' | asset_url | stylesheet_tag }}
</head>
<body class="template-{{ template.name }}">
    <main role="main">
        {{ content_for_layout }}
    </main>
</body>
</html>